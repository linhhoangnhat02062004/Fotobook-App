<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fotobook Sign Up</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= csrf_meta_tags %>
</head>
<body>
   <!-- Header -->
   <nav class="navbar navbar-expand-lg navbar-dark bg-primary py-1 px-5">
      <div class="container-fluid">
          <a class="navbar-brand fw-bold" href="<%= root_path %>" style="font-size:18px;">Fotobook</a>
          <form class="d-flex mx-2 flex-grow-1" style="max-width:600px;">
              <input class="form-control form-control-sm" type="search" placeholder="Search Photo / Album" aria-label="Search">
          </form>
          <div class="ms-auto">
              <a href="<%= new_user_session_path %>" class="text-white fw-bold me-2" style="font-size:14px; text-decoration: none;">Login</a>
          </div>
      </div>
  </nav>
  
  <!-- Main Content -->
  <div class="container-fluid main-container d-flex align-items-center justify-content-center min-vh-90">
      <div class="row justify-content-center w-100">
          <div class="col-9 col-md-6 col-lg-4">
              <div class="signup-section text-center">
                  <h1 class="signup-title display-1 mb-4">Fotobook Sign Up</h1>   
                  
                  <!-- Social Login Buttons -->
                  <div class="social-login-container d-flex justify-content-center gap-3 mb-2 p-3 bg-light rounded-2 border border-2 shadow-sm mx-auto" style="max-width: 410px;">
                      <div class="d-flex justify-content-center gap-5">
                          <button class="btn social-btn google-btn bg-light rounded-circle d-flex align-items-center justify-content-center shadow-sm" style="width: 35pt; height: 35pt;">
                              <%= image_tag 'logoGoogle.svg.png', alt: 'LogoGoogle', width: 50, height: 50 %>
                          </button>
                          <button class="btn social-btn facebook-btn bg-light rounded-circle d-flex align-items-center justify-content-center shadow-sm" style="width: 35pt; height: 35pt;">
                              <%= image_tag 'iconFB.svg', alt: 'LogoFacebook', width: 50, height: 50 %>
                          </button>
                          <button class="btn social-btn twitter-btn bg-light rounded-circle d-flex align-items-center justify-content-center shadow-sm" style="width: 35pt; height: 35pt;">
                              <%= image_tag 'Logo_of_Twitter.svg.png', alt: 'LogoX', width: 48, height: 48 %>
                          </button>
                      </div>
                  </div>
                  
                  <!-- Signup Form -->
                  <div class="signup-form-container border border-2 bg-light rounded-2 shadow-sm pt-3 pb-4 p-md-4 mx-auto" style="max-width: 410px;">
                      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: 'needs-validation', novalidate: true, id: 'signupForm' }) do |f| %>
                        <%= render "devise/shared/error_messages", resource: resource %>
                        
                        <div class="mt-2 mb-3 px-1">
                            <%= f.label :first_name, class: 'form-label fw-semibold text-dark text-start w-100' %>
                            <%= f.text_field :first_name, 
                                class: 'form-control form-control-lg rounded-2 border-2', 
                                placeholder: 'First Name', 
                                required: true %>
                            <div class="invalid-feedback">
                                Vui lòng nhập tên của bạn.
                            </div>
                        </div>
                        
                        <div class="mb-3 px-1">
                            <%= f.label :last_name, class: 'form-label fw-semibold text-dark text-start w-100' %>
                            <%= f.text_field :last_name, 
                                class: 'form-control form-control-lg rounded-2 border-2', 
                                placeholder: 'Last Name', 
                                required: true %>
                            <div class="invalid-feedback">
                                Vui lòng nhập họ của bạn.
                            </div>
                        </div>
                        
                        <div class="mb-3 px-1">
                            <%= f.label :email, class: 'form-label fw-semibold text-dark text-start w-100' %>
                            <%= f.email_field :email, 
                                class: 'form-control form-control-lg rounded-2 border-2', 
                                placeholder: 'Email Address', 
                                autocomplete: 'email',
                                required: true %>
                            <div class="invalid-feedback">
                                Vui lòng nhập email hợp lệ.
                            </div>
                        </div>
                        
                        <div class="mb-3 px-1">
                            <%= f.label :password, class: 'form-label fw-semibold text-dark text-start w-100' %>
                            <%= f.password_field :password, 
                                class: 'form-control form-control-lg rounded-2 border-2', 
                                placeholder: 'Password', 
                                autocomplete: 'new-password',
                                required: true %>
                            <div class="invalid-feedback">
                                Vui lòng nhập mật khẩu.
                            </div>
                            <% if @minimum_password_length %>
                                <div class="form-text">
                                    <i class="fas fa-info-circle me-1"></i>
                                    Tối thiểu <%= @minimum_password_length %> ký tự
                                </div>
                            <% end %>
                        </div>
                        
                        <div class="mb-3 px-1">
                            <%= f.label :password_confirmation, class: 'form-label fw-semibold text-dark text-start w-100' %>
                            <%= f.password_field :password_confirmation, 
                                class: 'form-control form-control-lg rounded-3 border-2', 
                                placeholder: 'Confirm Password', 
                                autocomplete: 'new-password',
                                required: true %>
                            <div class="invalid-feedback">
                                Vui lòng xác nhận mật khẩu.
                            </div>
                        </div>
                        
                        <div class="mb-2 d-grid px-1">
                            <%= f.submit "Sign Up", 
                                class: 'mt-2 btn signup-btn btn-primary btn-lg rounded-2 fw-bold shadow-sm',
                                style: 'padding: 0.7rem 0rem;' %>
                        </div>
                      <% end %>
                      
                      <!-- Login Link -->
                      <div class="mt-3 text-center">
                          <p class="mb-0 text-muted">
                              Đã có tài khoản? 
                              <%= link_to "Đăng nhập", new_user_session_path, class: 'text-primary text-decoration-none fw-semibold' %>
                          </p>
                      </div>
                  </div>
              </div>
          </div>
      </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <%= javascript_include_tag 'application' %>
</body>
</html>
