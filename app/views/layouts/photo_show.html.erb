<!DOCTYPE html>
<html lang="vi">
<head>
  <title><%= content_for(:title) || "Fotobook" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body style="background-color: #f0f2f5; margin: 0; padding: 0;">
  <!-- Flash Messages -->
  <% if notice %>
    <div class="alert alert-success alert-dismissible fade show fade-in" role="alert" style="position: fixed; top: 6px; right: 6px; z-index: 9999; padding: 2px 8px; font-size: 12px; line-height: 1.1; border-radius: 4px; min-width: 0;">
      <i class="fas fa-check-circle me-1" style="font-size: 13px"></i><%= notice %>
      <button type="button" class="btn-close btn-close-sm" data-bs-dismiss="alert" aria-label="Close" style="padding: 0 2px; margin: 0 0 0 6px; font-size: 10px; height: 16px; width: 16px; line-height: 1;"></button>
    </div>
  <% end %>

  <% if alert %>
    <div class="alert alert-danger alert-dismissible fade show fade-in" role="alert" style="position: fixed; top: 6px; right: 6px; z-index: 9999; padding: 2px 8px; font-size: 12px; line-height: 1.1; border-radius: 4px; min-width: 0;">
      <i class="fas fa-exclamation-circle me-1" style="font-size: 13px"></i><%= alert %>
      <button type="button" class="btn-close btn-close-sm" data-bs-dismiss="alert" aria-label="Close" style="padding: 0 2px; margin: 0 0 0 6px; font-size: 10px; height: 16px; width: 16px; line-height: 1;"></button>
    </div>
  <% end %>

  <%= yield %>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  <%= javascript_include_tag 'application' %>
</body>
</html> 