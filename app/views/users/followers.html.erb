<div class="container mt-4">
  <div class="row">
    <div class="col-md-8 mx-auto">
      <div class="card">
        <div class="card-header">
          <h4 class="mb-0">
            <i class="fas fa-users me-2"></i>
            <%= @user.first_name %>'s Followers (<%= @followers.count %>)
          </h4>
        </div>
        <div class="card-body">
          <% if @followers.any? %>
            <div class="row">
              <% @followers.each do |follower| %>
                <div class="col-md-6 mb-3">
                  <div class="d-flex align-items-center p-3 border rounded">
                    <div class="me-3">
                      <% if follower.avatar.present? %>
                        <%= image_tag follower.avatar.url, class: 'rounded-circle', width: 50, height: 50, style: 'object-fit: cover;' %>
                      <% else %>
                        <div class="rounded-circle bg-secondary d-flex align-items-center justify-content-center" style="width: 50px; height: 50px;">
                          <i class="fas fa-user text-white"></i>
                        </div>
                      <% end %>
                    </div>
                    <div class="flex-grow-1">
                      <h6 class="mb-1"><%= follower.first_name %> <%= follower.last_name %></h6>
                      <small class="text-muted"><%= follower.email %></small>
                    </div>
                    <div>
                      <%= link_to user_path(follower), class: 'btn btn-sm btn-outline-primary' do %>
                        <i class="fas fa-eye me-1"></i>View Profile
                      <% end %>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
          <% else %>
            <div class="text-center py-4">
              <i class="fas fa-users fa-3x text-muted mb-3"></i>
              <p class="text-muted">No followers yet.</p>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div> 